/**
 * Trust stats section — intentionally removed.
 * Replaced by trust bar inside the hero section per the approved mockup.
 * This export is kept as a no-op to avoid breaking imports.
 */
export function TrustStats() {
  return null;
}
